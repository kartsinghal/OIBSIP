import MenuSection from '../components/MenuSection';

export default function MenuPage() {
  return (
    <main style={{ minHeight: '80vh', backgroundColor: 'var(--bg-primary)', paddingTop: 40 }}>
      <MenuSection />
    </main>
  );
}
